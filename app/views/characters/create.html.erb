Rails.application.routes.draw do
    root "home#index"
  
    resources :worlds, only: [:index, :show] do
      resources :characters, only: [:index, :new, :create]
    end
  
    # キャラの個別機能は残す
    resources :characters, only: [:show, :edit, :update, :destroy]
  end
  